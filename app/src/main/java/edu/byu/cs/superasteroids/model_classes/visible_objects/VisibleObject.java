package edu.byu.cs.superasteroids.model_classes.visible_objects;

/**
 * Created by Marshall Garey
 * Visible objects are things that the user can see.
 */
public class VisibleObject {

    public VisibleObject() {}

    /**
     * Updates the positions (x and y coordinates) of the image
     */
    public void update() {
        return;
    }

    /**
     * Draws the object on the screen according to its x and y positions. Does not draw the object
     * if it is not in the viewport; in other words, if the user cannot see it, there is no need
     * to draw it.
     */
    public void draw(int x, int y) {
        return;
    }
}
